<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="tcc.cerva.toiss.android"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name="CervaToiss"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaCerveja" android:label="CERVEJA" />
        <activity android:name=".ListaValor" android:label="VALOR" />
        <activity android:name=".AddCerveja" android:label="ADICIONAR" />
        <activity android:name=".AddValor" android:label="ADICIONAR" />
        <activity android:name=".SincronizarServidor" android:label="SERVIDOR" />
    </application>
</manifest>
